/**
 * 這一個 hook ，已經很像是 angular 的 某功能 api-service 了
 */

// Create an instance of the openapi-react-query client
// apiClient is base api tool, turn it to openapi-react-query client
// openapi-react-query combine openapi-fetch and react query
// const client = createClient(apiClient)

// Example: A hook to fetch a greet TODO:
// export const useGreets = () => {
//   return client.useQuery('get', '/api/greet', {
//     params: {
//       path: { post_id: 5 },
//     },
//   })
// }
