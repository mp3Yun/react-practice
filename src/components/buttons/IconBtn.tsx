function IconBtn(props: BaseButton) {
  const icon = props.iconSrc || 'default'
  return (
    <button>
      {/* TODO: add Icon */}
      not done icon {icon}
    </button>
  )
}

export default IconBtn
